# ECHO TOWER v0.6.1 deployment

<a href="#english">English</a> · <a href="#korean">한국어</a>

<a id="english"></a>
## English

Production verification completed on **2026-09-19 at 06:41 UTC** for
`https://clawd-game.whchoi.net/`. The title reports **v0.6.1 · build bcef57a6**.
The crab player is now an orange cat with a mint scarf, including eight skins,
echoes, portraits, dash afterimages, app icons and social previews.

### Deployment

| Item | Observed result |
|---|---|
| Feature commit | `df102dc` |
| Stack / region | `ClawdEchoTowerStack` / `ap-northeast-2` |
| CloudFormation | `UPDATE_COMPLETE` |
| ECS task definition | Revision 10 → 11; two running, zero pending; rollout `COMPLETED` |
| Load balancer targets | Two healthy |
| Stack alarms | 11 `OK`, zero `ALARM` |
| Container | ARM64, 175,816,891 bytes |
| Client build | `bcef57a6`, identical in the local build and container build |
| Simulation / generator | SIM 4 / GEN 2, unchanged |
| CloudFront invalidation | `I7F1QJQ5IL8F4GHI433WCLR4GS`, `Completed` |

The reviewed stack difference changed the task's application image and
`APP_VERSION`. The release used `npm run release -- patch --no-tag`; it prepared
the version before building and did not create a Git tag.

### Verification

| Check | Result |
|---|---|
| Unit, integration and native browser audio tests | 1,727 / 1,727; 90 files; zero skipped |
| Standalone `npm run qa:audio` | 1 / 1 |
| Typecheck and generated-level check | Passed |
| Production routes, headers, API versions and direct ALB access blocking | 13 / 13 |
| Referenced JS/CSS requests | 2 assets × 20 requests, all HTTP 200 and edge hits |
| Deployed file digests | 15 / 15 match the local production build |
| Live Chromium smoke | 23 / 23, zero issues, including offline play |
| Live phone/tablet emulation | 54 / 54, zero issues |
| Chromium replay corpus | 18 / 18 match the Node baseline |
| Live replay submission and cleanup | 7 / 7 checks |

Native audio uses Chromium's `OfflineAudioContext`: muted SFX, muted music
tracks and master mute render silence, while the other unmuted category remains
audible. This test ran separately and again in the release suite with
`CLAWD_BROWSER_TESTS=1`.

One locally verified t1 replay was submitted under a temporary player. Acceptance,
idempotent resubmission, echo retrieval, the uncached personal row and rejection
of a tampered claim all passed. A conditional transaction removed only that
player's RUN, leaderboard, best and hash rows and reversed its board-counter
increment. Consistent reads found all four rows absent; the temporary echo then
returned 404.

The smoke deliberately emits `smoke-forced-js-error` to verify telemetry and
disconnects the network to verify offline play. Those expected events are
separate from the zero unexpected application issues.

### Documentation sync

The sync resolved missing descriptions of the shared `PORTRAIT_FEET` anchor,
the SVG → PNG → social capture → rebuild workflow, and standalone native audio
checks. README, architecture, game references, QA instructions, current version,
payload and deployment links now agree with source and the observed release.
The document/link and staged-change checks passed with zero findings.

CLAUDE-specific quality scores and the repository skill inventory were **N/A
before and after**: this repository has `AGENTS.md`, no `CLAUDE.md` and no
repository `skills/` directory. No score or new architecture decision was
invented for a presentation change. Older dated reports remain intact.

Client payload: **548.14 KiB JS / 168.16 KiB gzip**, measured with Node's default
`gzipSync`; CSS is **89.62 KiB / 20.75 KiB gzip**.

[Deployment data](2026-09-19-release-0.6.1.json) ·
[Live title](assets/2026-09-19-cat-title.png) ·
[Live play](assets/2026-09-19-cat-play.png)

<a id="korean"></a>
## 한국어

**2026-09-19 06:41 UTC**, `https://clawd-game.whchoi.net/`의 운영 검증을
완료했습니다. 타이틀 표시는 **v0.6.1 · 빌드 bcef57a6**입니다.
게 플레이어를 민트색 스카프를 두른 주황색 고양이로 교체했고, 여덟 스킨,
메아리, 초상, 대시 잔상, 앱 아이콘과 공유 미리보기에 반영했습니다.

### 배포

| 항목 | 확인 결과 |
|---|---|
| 기능 커밋 | `df102dc` |
| 스택 / 리전 | `ClawdEchoTowerStack` / `ap-northeast-2` |
| CloudFormation | `UPDATE_COMPLETE` |
| ECS 태스크 정의 | 리비전 10 → 11; 실행 2개, 대기 0개; 롤아웃 `COMPLETED` |
| 로드밸런서 대상 | 2개 healthy |
| 스택 알람 | 11개 `OK`, `ALARM` 0개 |
| 컨테이너 | ARM64, 175,816,891바이트 |
| 클라이언트 빌드 | `bcef57a6`, 로컬 빌드와 컨테이너 빌드가 동일 |
| 시뮬레이션 / 생성기 | SIM 4 / GEN 2 유지 |
| CloudFront 무효화 | `I7F1QJQ5IL8F4GHI433WCLR4GS`, `Completed` |

검토한 스택 차이는 태스크의 애플리케이션 이미지와 `APP_VERSION` 변경입니다.
`npm run release -- patch --no-tag`로 빌드 전에 버전을 준비했으며
Git 태그는 생성하지 않았습니다.

### 검증

| 검사 | 결과 |
|---|---|
| 단위·통합·실제 브라우저 오디오 검사 | 1,727 / 1,727; 90개 파일; 건너뜀 0개 |
| 별도 `npm run qa:audio` | 1 / 1 |
| 타입·생성 레벨 최신성 | 통과 |
| 운영 경로·헤더·API 버전·직접 ALB 접근 차단 | 13 / 13 |
| 참조 JS/CSS 요청 | 에셋 2개 × 20회, 모두 HTTP 200·엣지 캐시 적중 |
| 운영 파일 다이제스트 | 15 / 15 로컬 운영 빌드와 일치 |
| 운영 Chromium 스모크 | 23 / 23, 문제 0개, 오프라인 플레이 포함 |
| 운영 휴대폰·태블릿 모사 | 54 / 54, 문제 0개 |
| Chromium 리플레이 코퍼스 | 18 / 18 Node 기준과 일치 |
| 운영 리플레이 제출·정리 | 7 / 7 검사 |

오디오 검사는 Chromium의 `OfflineAudioContext`를 사용합니다. 효과음·각 음악
트랙·전체 음량을 껐을 때 무음이 출력되고, 다른 켜진 채널에서는 소리가
출력되는지 확인했습니다. 별도 검사와 `CLAWD_BROWSER_TESTS=1`을 지정한
전체 릴리스 검사에서 각각 실행했습니다.

로컬에서 검증한 t1 리플레이 한 건을 임시 플레이어로 제출했습니다.
접수, 동일 기록 재제출의 멱등성, 메아리 조회, 캐시하지 않는 개인 기록과
변조한 주장 값의 거절을 확인했습니다. 조건부 트랜잭션으로 해당 플레이어의
RUN·순위표·최고 기록·해시 행만 삭제하고 보드 카운터 증가분을 되돌렸습니다.
일관된 읽기로 네 행의 삭제를 확인했으며 임시 메아리는 이후 404를 반환했습니다.

스모크는 텔레메트리를 확인하려고 `smoke-forced-js-error`를 의도적으로
발생시키고, 오프라인 플레이를 확인하려고 네트워크를 끊습니다.
이 예상된 이벤트와 별개로 예상 밖 애플리케이션 문제는 0개였습니다.

### 문서 동기화

공유 초상 기준점 `PORTRAIT_FEET`, SVG 수정 → PNG 생성 → 공유 이미지 캡처 →
재빌드 흐름, 별도 실제 오디오 검사의 누락된 설명을 보완했습니다.
README·아키텍처·게임 참조·QA 절차·현재 버전·용량·배포 링크를 코드와 실제
배포 결과에 맞췄습니다. 문서·링크와 스테이지 검사는 문제 0개로 통과했습니다.

CLAUDE 전용 품질 점수와 저장소 스킬 목록 검사는 **전후 모두 해당 없음**입니다.
이 저장소는 `AGENTS.md`를 사용하며 `CLAUDE.md`와 저장소 `skills/` 디렉터리가
없습니다. 표현 변경을 이유로 점수나 새 아키텍처 결정을 만들지 않았으며,
이전 날짜의 검증 기록도 보존했습니다.

클라이언트 용량은 Node 기본 `gzipSync` 기준 **JS 548.14 KiB / gzip 168.16 KiB**,
**CSS 89.62 KiB / gzip 20.75 KiB**입니다.

[배포 검증 데이터](2026-09-19-release-0.6.1.json) ·
[운영 타이틀](assets/2026-09-19-cat-title.png) ·
[운영 플레이](assets/2026-09-19-cat-play.png)
